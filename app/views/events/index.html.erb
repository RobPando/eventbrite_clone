<% provide(:title, "Events") %>

<h1>All the Events</h1>

<% if @events.any? %>
<ul>
  <% @events.each do |event| %>
  <li>
    <%= link_to event.name, event_path(event) %>
  </li>
  <% end %>
</ul>
<% end %>